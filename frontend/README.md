# Frontend — Tickt Web App

Frontend-часть веб-приложения для управления списком задач.

## 🚀 Запуск

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## 🔧 Устранение проблем

### Ошибка с Rollup на Windows

Если при запуске `npm run dev` возникает ошибка:

```
Error: Cannot find module @rollup/rollup-win32-x64-msvc
```

**Решение:**

1. Удалите `node_modules` и `package-lock.json`:

   **PowerShell:**

   ```powershell
   Remove-Item -Recurse -Force node_modules, package-lock.json
   ```

   **Command Prompt:**

   ```cmd
   rmdir /s /q node_modules
   del package-lock.json
   ```

2. Переустановите зависимости:

   ```bash
   npm install
   ```

3. Запустите проект:
   ```bash
   npm run dev
   ```

> **Примечание:** Если папка не удаляется, закройте все окна редактора/терминала, которые могут использовать эту папку.

## 📝 Скрипты

| Команда           | Описание                         |
| ----------------- | -------------------------------- |
| `npm run dev`     | Запуск dev-сервера с hot-reload  |
| `npm run build`   | Сборка приложения для production |
| `npm run preview` | Предпросмотр production-сборки   |
| `npm run lint`    | Проверка кода с помощью ESLint   |
